__pycache__/
*.pyc
*.db
*.sqlite
*.db-shm
*.db-wal
*.db-journal
agent_db.sqlite
flow_agent.db
.env
# Local secret backups produced by scripts/setup-postiz-runtime.ps1 — never commit.
.env.backup-*
.env.bak
.env.bak.*
output/
youtube
agent/active_project.json

# Generated AI tool configs (run: python setup.py)
.claude/commands/fk:*
.gemini/commands/
AGENTS.md
GEMINI.md
.fk-setup.json
.venv/
.pytest_cache/
.local-agent/

# Local cutout engine model cache — SHA-pinned BiRefNet ONNX weights, staged by
# scripts/prepare_cutout_model.py. Repo-root anchored so it never matches the
# agent/models/ Python package. Hundreds of MB — NEVER commit.
/models/

# PR #298 hermetic A/B harness temp roots (never commit)
docs/evidence/pr298_ab/

# Generated extension build stamp (run: node scripts/stamp-extension-build.mjs).
# NEVER commit: a committed stamp is stale the moment the next commit lands, which
# is exactly the stale-provenance defect it was introduced to remove. Absent is
# fine — background.js imports it in try/catch and reports stamped:false.
extension/build-stamp.js

# Dependencies and Scratch
node_modules/
scratch/
*.log

# Artifacts and Payloads
diag.json
diagnostic*.json
bosmax_search*.json
smoke_body.json
upload_body.json
verify*.json
f2v_job_body.json
*_body.json
test_image.jpg
test_image_b64.txt
data/products/images/*.jpg
data/products/images/*.png
data/products/images/*.jpeg
data/products/images/*.webp
data/exact-product/
data/product-reference-packs/
data/product_registration/intake_images/
data/product_registration/intake_media/
data/fastmoss/imports/
data/product_knowledge/imports/
data/product_registration/drafts/
data/prompt_library/
outputs/
scripts/browser_evidence/
_copy_register_v2_*
_delivery_video_fix/
_phase4_*
_postmerge_video_validation/

# Local tooling / debug artifacts (machine-specific; do not commit)
.mcp.json
.playwright-mcp/

# Copywriting landbank uploads (runtime content, ADR-008)
data/copywriting_landbank/*.csv
data/avatar_registry/*.csv
data/scene_context_registry/*.csv

# Avatar Registry CSV Factory staging batches (runtime artifacts, ADR-008)
data/avatar_registry/csv_factory/

# Phase 2A compositor spike — generated image outputs (regenerated by the script)
scripts/fixtures/poster-compositor/*.png
scripts/fixtures/poster-compositor/archetypes/*.png

# Per-deploy production runtime provenance stamp (written by
# scripts/deploy-canonical-release.ps1). NEVER committed — it is release-local.
runtime_manifest.json

# BOSMAX Browser UAT machine-local runtime (never commit profile/state)
_bosmax_runtime/browser_uat/chrome-profile/
_bosmax_runtime/browser_uat/logs/
_bosmax_runtime/browser_uat/state/
_bosmax_runtime/browser_uat/screenshots/
_bosmax_runtime/browser_uat/traces/
**/browser_uat/chrome-profile/
**/browser_uat/logs/
**/browser_uat/state/
**/browser_uat/screenshots/
**/browser_uat/traces/
